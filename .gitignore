# IDEs
.idea/

# Hugo
resources/
public/
jsconfig.json
node_modules/
go.sum
.hugo_build.lock
.DS_Store


# Svamp loop/session scratch (binaries, loop state)
.svamp/

# Loop-session-specific (reference .svamp loop scripts)
.claude/settings.json
.claude/settings.local.json
.claude/agents/loop-evaluator.md

# Newsletter raw source captures (keep sources.md, ignore bulky downloads)
data/newsletter/**/*.pdf
data/newsletter/**/*.html
data/newsletter/**/*.txt

# Never commit Slack/secret tokens
*.env
!*.env.example
lab-slack.env

# Sensitive/internal lab memory — NEVER commit (gitignored)
*.local.md
.claude/skills/aicell-website/lab-notes.local.md

public_preview/

# Bulk interview captures (raw photos + notes) — select/resize into content/ when writing the post
interviews/
